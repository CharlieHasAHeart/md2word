services:
  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
      target: prod
    container_name: multi-space-frontend
    ports:
      - "8080:80"
    restart: unless-stopped
